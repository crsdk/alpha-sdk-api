/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/api/server/status": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Server health check
         * @description Returns server health, uptime, version info, and connected camera count.
         *     Used by the TypeScript `ServerManager` to confirm the server is ready after spawning the binary.
         */
        get: operations["getServerStatus"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/server/logs": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get recent server logs
         * @description Returns buffered server log output. Useful for debugging connection issues,
         *     SDK errors, and property change confirmations without accessing the terminal.
         */
        get: operations["getServerLogs"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/server/shutdown": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Graceful server shutdown
         * @description Initiates graceful shutdown: disconnects all cameras, closes SSE
         *     connections, then exits.
         */
        post: operations["shutdownServer"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List available cameras
         * @description Enumerate all cameras connected via USB and network.
         */
        get: operations["listCameras"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/connection": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Check connection status
         * @description Returns the current connection state for a camera (connected/disconnected,
         *     model, id). When connected, `data.mode` reports the active SDK
         *     connection mode (`remote` / `remote-transfer` / `contents`).
         */
        get: operations["getConnectionStatus"];
        put?: never;
        /**
         * Connect to camera
         * @description Establish connection in the specified mode. Defaults to `remote` if no mode provided.
         */
        post: operations["connectCamera"];
        /**
         * Disconnect camera
         * @description Cleanly disconnects from the specified camera. Server emits a synthetic
         *     `disconnected` SSE event before clearing internal callbacks.
         */
        delete: operations["disconnectCamera"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/properties/{propertyName}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get a camera property
         * @description Returns the current value, human-readable format, writability flag, and all
         *     values the camera currently accepts for this property.
         *
         *     ## `writable` and `available_values` are a snapshot, not a spec
         *
         *     Both describe the camera **right now**, and both move as other settings
         *     change. Two things routinely surprise callers:
         *
         *     **The priority key gates writability.** Read a property while the camera
         *     holds `camera-position` and most of the body reports `writable: false`
         *     with an empty `available_values`. Claim `pc-remote` first — see
         *     `setPriorityKey` — or you will conclude a camera cannot do things it can.
         *     Measured on an ILCE-7M5: `focus-area` went from `writable: false` with no
         *     values to `writable: true` with 24 values, purely from the priority key.
         *
         *     **Other properties gate the value list.** `focus-area` is the clearest
         *     case: its Tracking variants only exist while `focus-mode` is a continuous
         *     mode. On an ILCE-1M2 in `AF_S` the list has 12 entries and no Tracking
         *     entries at all, so setting `0x17` (`Tracking Expand Flexible Spot`) is
         *     rejected as not available — correctly, because at that moment it is not.
         *     Switch `focus-mode` to `AF_C` and the Tracking variants appear.
         *
         *     So do not cache these fields, and do not treat a value's absence as
         *     "this model cannot do it". Re-read the property after changing anything
         *     related, and prefer a value taken from the live `available_values` over a
         *     constant from another body.
         */
        get: operations["getProperty"];
        /**
         * Set a camera property
         * @description Set a camera property value. Accepts hex values (from GET response `available_values`)
         *     or human-readable strings (e.g. `"f/5.6"`, `"1/250"`, `"wide"`).
         *
         *     The server validates against the camera's current available values and checks
         *     writability before sending to the SDK.
         */
        put: operations["setProperty"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/properties/all": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get all camera properties in one call
         * @description Returns every property the camera currently reports, deduplicated by name
         *     (the SDK occasionally returns multiple internal entries for the same
         *     property — the server picks the entry that has values or is writable).
         *
         *     ## Important: response shape differs from single-property GET
         *
         *     The per-property entries here use a DIFFERENT shape than
         *     `getProperty` — see `BulkPropertyEntry` vs `PropertyData`. Highlights:
         *
         *     - Field names: `current_value` / `current_hex_value` / `current_formatted`
         *       instead of `value` / `formatted`
         *     - `writable` is a real boolean here (not the stringified `"true"`/`"false"`
         *       you get from single GET)
         *     - `available_values` entries carry both numeric `value` and `hex_value`
         *
         *     Use this endpoint for "load everything once" UI flows. Use single GET
         *     when you need just one property and want the consistent setter format.
         */
        get: operations["getAllProperties"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/af-area-position": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get the AF frame position
         * @description Return the camera's current autofocus frame(s) — position, size and focus
         *     state — so a client can draw a focus box over a live view.
         *
         *     Each frame reports its centre three ways: `normalized` (0-1, the value to
         *     use for overlays at any resolution), `position` (the same point in the
         *     coordinate space `setAFAreaPosition` expects, so a read can be written
         *     straight back), and `raw` (the camera's own numerator/denominator pair).
         *     `size.normalized_width` / `normalized_height` give the box dimensions as a
         *     fraction of the frame.
         *
         *     **More than one frame can be returned.** An expanding area reports the
         *     main box plus its assist box — an ILCE-7M4 set to Tracking Expand Flexible
         *     Spot returns a 38x38 `contrast-flexible-main` and a 76x76
         *     `contrast-flexible-assist`. Draw them all.
         *
         *     `state` (`focused`, `not-focused`, `moving`, `selection`, …) is the value
         *     to drive box colour from.
         *
         *     ## When this returns 400
         *
         *     The camera only reports a frame for focus areas that actually draw a box —
         *     Flexible Spot, Expand Flexible Spot, Tracking variants. In Wide or a
         *     face/eye-priority mode there is no movable frame and nothing is reported.
         *     Live view should be streaming, since this reads a live-view property.
         */
        get: operations["getAFAreaPosition"];
        /**
         * Move the AF frame
         * @description Move the centre of the autofocus frame. Send either absolute coordinates
         *     (`x` 0-639, `y` 0-479) or `normalized` 0-1 values — the latter is usually
         *     what you want when translating a tap or click on a live view.
         *
         *     ## The camera clamps, and does not tell you
         *
         *     The usable area is smaller than the full range: it is inset from the edges
         *     and varies by model, aspect ratio and AF setting. A request inside
         *     0-639/0-479 is always accepted with `200`, then silently clamped.
         *
         *     Measured on an ILCE-7M4 with Tracking Expand Flexible Spot — every corner
         *     was requested and every one was pulled inward, symmetrically:
         *
         *     | Requested | Landed |
         *     |---|---|
         *     | `0, 0` | `65, 61` |
         *     | `639, 0` | `574, 61` |
         *     | `639, 479` | `574, 418` |
         *     | `0, 479` | `65, 418` |
         *     | `320, 240` | `320, 240` |
         *
         *     That is a usable area of x `65-574`, y `61-418` — the middle 80% x 74% of
         *     the coordinate space. Treat those numbers as illustrative, not a contract:
         *     a different body, aspect ratio or AF area will inset differently.
         *
         *     A write is also ignored outright when the focus area has no movable box.
         *     **Always follow up with `getAFAreaPosition` to see where the frame
         *     actually is** rather than assuming the requested value took.
         *
         *     Requires priority key `pc-remote`.
         */
        put: operations["setAFAreaPosition"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/fingerprint": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get the camera's authentication fingerprint
         * @description Return the fingerprint of a camera that uses **access authentication**,
         *     for use as `auth.fingerprint` when connecting.
         *
         *     Read this rather than transcribing the value from the camera's network
         *     screen. The value here comes from the SDK, which is what the connect
         *     check compares against, and a hand-copied value is easy to get subtly
         *     wrong — a single missing `=` of base64 padding fails with `0x8215`.
         *
         *     Works **before** connecting, which is the point: the fingerprint is an
         *     input to `connectCamera`, not a result of it. The camera only needs to
         *     have been discovered.
         *
         *     ## Cameras without access authentication
         *
         *     Returns `200` with `ssh_supported: false` and no `fingerprint`. That is
         *     not an error — USB connections and bodies with authentication off need no
         *     fingerprint at all.
         */
        get: operations["getFingerprint"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/tracking-frame": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get the tracking frame
         * @description Return the box the camera is tracking a subject in, after a
         *     `remoteTouch`. The shape is the same as `getAFAreaPosition` — `normalized`
         *     for overlays, `position` in the coordinate space `remoteTouch` accepts,
         *     `raw` as the camera reports it — so the same overlay code renders both.
         *
         *     Poll this to follow a moving subject: the camera updates the frame on its
         *     own once tracking has been seeded.
         *
         *     `type` is `target-af` for the subject being tracked, `non-target-af` for a
         *     candidate the camera is showing but not acting on.
         *
         *     ## Empty is a normal answer
         *
         *     With nothing being tracked this returns `200` with `frames: []` and
         *     `available: true` — the property was readable, there was just no subject.
         *     A `400` means the property could not be read at all, usually because live
         *     view is not running.
         *
         *     Two ways to get a frame here, both measured on an ILCE-7M4:
         *
         *     - `remoteTouch`, with the body's **Touch Func. in Shooting** set to
         *       `Touch Tracking`. Tracking then holds on its own with no button held.
         *     - The camera tracking for its own reasons — a tracking focus area with AF
         *       engaged. Holding `af-on` in `Tracking: Expand Flexible Spot` reports a
         *       `target-af` frame that disappears on release.
         *
         *     With the ILCE-7M4 default of `Touch Focus`, a touch moves the AF frame
         *     instead and nothing appears here — read `getAFAreaPosition` in that case.
         */
        get: operations["getTrackingFrame"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/priority-key": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get priority key setting
         * @description Returns the current priority key setting (`pc-remote` or `camera-position`).
         *
         *     **Note**: FX3 / cinema bodies do not implement `PriorityKeySettings` —
         *     GET returns empty data and SET returns SDK error `0x8402`. These bodies
         *     auto-enter PC Remote mode on USB.
         */
        get: operations["getPriorityKey"];
        /**
         * Set priority key
         * @description Set the priority key. Must be `pc-remote` for the camera to accept remote
         *     property changes and shooting commands. Not supported on FX3 / cinema
         *     bodies (returns 400).
         */
        put: operations["setPriorityKey"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/actions/shutter": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Take a photo
         * @description Single shot (no body or empty body), or continuous shooting with `down`/`up` control.
         *     For continuous shooting, set drive mode to a continuous mode first.
         */
        post: operations["triggerShutter"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/actions/button": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Press a button on the camera body
         * @description Press a physical button — menu, enter, the D-pad, C1-C7, playback and so
         *     on — as if someone pressed it on the body. Combined with live view and
         *     `getOSDFrame` this is enough to drive the camera's own menus remotely and
         *     read the result back off the screen.
         *
         *     Omit `action` for a full press. Send `down` and a later `up` to hold a
         *     button, the same contract as `triggerShutter`.
         *
         *     ## Not every body exposes every button
         *
         *     The camera reports which keys it will accept, and that list is much
         *     shorter than the full enum. An ILCE-7M4 reports 24: the D-pad and
         *     multi-selector, `enter`, `menu`, `fn`, `playback`, `c1`-`c4`, `movie`,
         *     `ael` and `af-on` — but **not** `delete`, `mode`, `c5`-`c7`, `display`,
         *     `home`, `clips`, `slot-select`, `cancel-back` or `thumbnail`.
         *
         *     A rejected press returns `400` with the camera's own supported list in
         *     `data.supported_buttons`, so you can discover the real set at runtime.
         *
         *     ## Held keys block new presses
         *
         *     The SDK only starts a press while the camera reports itself idle, so a
         *     button already held — by this API or by a hand on the body — makes the
         *     next press fail. A release is never blocked: an `up` always goes through,
         *     so a held key can always be let go.
         *
         *     Requires priority key `pc-remote`.
         */
        post: operations["pressCameraButton"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/actions/touch": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Touch a point in the frame
         * @description Tap a point in the live-view frame, as if someone touched the camera's own
         *     screen there. Send absolute coordinates (`x` 0-639, `y` 0-479) or
         *     `normalized` 0-1 values — the latter is what you want for a tap or click
         *     on a live view.
         *
         *     ## This is not `setAFAreaPosition`
         *
         *     `setAFAreaPosition` drags the focus box to a point and leaves it there. A
         *     touch **seeds the camera's subject tracking**: the camera locks onto
         *     whatever is at that point and then follows it on its own. You cannot get
         *     that by rewriting positions from a client. Touch also works in focus areas
         *     that have no movable box — Wide, face priority — where
         *     `setAFAreaPosition` has nothing to move.
         *
         *     Read the result with `getTrackingFrame`.
         *
         *     ## The camera decides what a touch does
         *
         *     The SDK property that would select the touch behaviour
         *     (`FunctionOfRemoteTouchOperation`) is unsupported on Alpha bodies
         *     including the ILCE-7M4 — only broadcast bodies can choose. A touch does
         *     whatever the body's own **Touch Func. in Shooting** is set to
         *     (`MENU > Setup > Touch Operation > Touch Panel Settings > Shooting
         *     Screen`), and that setting is not exposed over this API:
         *
         *     | Touch Func. in Shooting | What `remoteTouch` does |
         *     |---|---|
         *     | `Touch Focus` (ILCE-7M4 default) | Moves the AF frame, like `setAFAreaPosition`. `getTrackingFrame` stays empty. |
         *     | `Touch Tracking` | Starts subject tracking at the point. `getTrackingFrame` reports the box. |
         *
         *     Both were measured on an ILCE-7M4. Note the touch is accepted **even when
         *     the body's `Touch Operation` is `Off`** — that setting governs the
         *     physical screen, not the remote property.
         *
         *     ## A 200 does not mean tracking started
         *
         *     The camera accepts the write and then decides. With `Touch Tracking` set,
         *     a touch on a point with nothing trackable — measured on flat wall and
         *     blown-out window areas — returns `200` and produces no tracking frame at
         *     all. **Always read `getTrackingFrame` to find out what actually
         *     happened.**
         *
         *     Touches land close to where they are asked for but not exactly: the
         *     camera snaps to the nearest feature it can hold. Measured on an ILCE-7M4,
         *     `320,240` landed at `320,239` and `200,300` at `209,297`, while points
         *     near the edges pulled further in — `0,479` landed at `35,445`.
         *
         *     Leave a beat between touches. Firing several within a second or so, or
         *     immediately after a `cancelRemoteTouch`, made the camera drop tracking
         *     entirely until it was left alone.
         *
         *     ## When this returns 400
         *
         *     Touch is gated on the camera reporting it as currently available. On the
         *     ILCE-7SM3 and ILCE-7C it is movie-mode only; other bodies gate it on focus
         *     mode or on live view running. A rejected touch says which gate failed.
         *
         *     Requires priority key `pc-remote`.
         */
        post: operations["remoteTouch"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/actions/touch-cancel": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Cancel a touch / stop tracking
         * @description Release the subject a `remoteTouch` locked onto. No body required.
         *
         *     The camera only accepts a cancel while it has something to cancel, so this
         *     returns `400` when nothing is being tracked. That is a safe no-op, not a
         *     failure to retry.
         *
         *     The gate is specifically about a **remote touch** being in progress, not
         *     about tracking in general. Tracking the camera started itself — a tracking
         *     focus area with `af-on` held — reports nothing to cancel, because there is
         *     no remote touch operation to release. The camera's ready flag also lags a
         *     successful cancel by a beat, so an immediate second call can still report
         *     success; treat `getTrackingFrame` as the truth.
         *
         *     Requires priority key `pc-remote`.
         */
        post: operations["cancelRemoteTouch"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/actions/half-press": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Focus lock (S1 half-press)
         * @description Half-press the shutter button to lock autofocus. No body required.
         */
        post: operations["halfPress"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/actions/af-shutter": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Auto-focus + capture
         * @description Autofocus then immediately capture in one operation. No body required.
         */
        post: operations["afShutter"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/actions/zoom": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Power zoom control
         * @description Control power zoom lenses. Requires a power zoom lens to be attached.
         */
        post: operations["controlZoom"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/actions/movie-rec": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Toggle movie recording
         * @description Start or stop movie recording. Server tracks the current state via the SDK
         *     and toggles it on each call. Camera must be in a movie shooting mode.
         */
        post: operations["toggleMovieRecording"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/actions/focus-near-far": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Step-based focus adjustment
         * @description Move focus toward near or far in discrete steps.
         *     Range: `-7` (near, max speed) to `+7` (far, max speed).
         *     Magnitude controls speed. `0` is not valid.
         */
        post: operations["focusNearFar"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/live-view/enable": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Enable live view on camera
         * @description Sends the SDK `Setting_Key_EnableLiveView` command to the camera.
         */
        post: operations["enableLiveView"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/live-view/disable": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Disable live view on camera
         * @description Sends the SDK `Setting_Key_DisableLiveView` command. Stops live view streaming if it was running.
         */
        post: operations["disableLiveView"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/live-view/status": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get live view status
         * @description Returns whether live view is `enabled` (camera setting) and whether
         *     the server is currently `streaming` frames from the camera.
         */
        get: operations["getLiveViewStatus"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/live-view/start": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Start frame capture
         * @description Starts the frame capture worker thread. Auto-enables live view if disabled.
         */
        post: operations["startLiveViewStream"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/live-view/stop": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Stop frame capture
         * @description Stops the frame capture worker thread. Live view itself remains enabled on the camera unless you also call `disableLiveView`.
         */
        post: operations["stopLiveViewStream"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/live-view/frame": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get single JPEG frame
         * @description Returns the latest live view frame as a JPEG image. Streaming must be
         *     started first via `liveView.start`. Poll this endpoint at ~15 fps
         *     (every ~66 ms) to render a live preview.
         */
        get: operations["getLiveViewFrame"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/live-view/osd-enable": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Enable OSD overlay
         * @description Enable On-Screen Display overlay via `CrDeviceProperty_OSDImageMode`.
         */
        post: operations["enableOSD"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/live-view/osd-disable": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Disable OSD overlay
         * @description Disables the camera's OSD overlay. Subsequent live view frames will not include UI overlays.
         */
        post: operations["disableOSD"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/live-view/osd-status": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get OSD status
         * @description Returns whether OSD is `supported` on this camera body and whether it is currently `enabled`.
         */
        get: operations["getOSDStatus"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/live-view/osd-frame": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get composited OSD frame
         * @description Returns a composited JPEG with live view image + camera UI overlay.
         *     Same polling pattern as `liveView.getFrame` — OSD must be enabled
         *     first via `liveView.enableOSD`.
         */
        get: operations["getOSDFrame"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/events": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Subscribe to all camera events (SSE)
         * @description **SDK generation note:** marked `x-fern-ignore: true` because
         *     Server-Sent Events don't map cleanly onto synchronous SDK methods.
         *     The TypeScript and Swift SDKs ship hand-written SSE clients that
         *     consume this endpoint and expose typed `EventStream` APIs.
         *
         *     ***
         *
         *     Server-Sent Events stream for all connected cameras. The server sends a
         *     keepalive comment (`: keepalive`) every 30 seconds and immediately fires
         *     an initial `connected` event with the subscription's camera ID payload
         *     (see the quirk note below).
         *
         *     ## Event Types
         *
         *     | Event | SDK Callback | Payload | Description |
         *     |-------|-------------|---------|-------------|
         *     | `connected` | `OnConnected` (or initial handshake) | `{cameraId, ...}` | Camera connection established **OR** initial SSE handshake (see quirk) |
         *     | `disconnected` | `OnDisconnected` | `{cameraId, reason?}` | Camera disconnected (real or synthetic — server emits one before clearing callbacks during a clean disconnect) |
         *     | `propertyChanged` | `OnPropertyChangedCodes` | `{cameraId, codes: [...]}` | One or more property values changed on the camera |
         *     | `afStatus` | `OnWarning` (`CrWarningExt_AFStatus`) | `{state: "focused" \| "unlocked" \| "tracking", source?}` | Autofocus state changed |
         *     | `downloadComplete` | `OnCompleteDownload` | `{cameraId, filename, savedPath}` | Auto-transferred image saved to host (remote mode + matching `still-image-store-destination`) |
         *     | `transferProgress` | `OnNotifyRemoteTransferResult` (or filesystem polling fallback) | `{cameraId, contentId, fileId, percent, savedPath?}` | Explicit file-pull progress (remote-transfer mode). On macOS V2.01 the SDK callback is unreliable; the server falls back to filesystem polling and emits synthetic events |
         *     | `contentsTransfer` | `OnContentsTransfer` | `{cameraId, files: [...]}` | Contents-transfer mode file list update (printable-ASCII filenames only) |
         *     | `operationResult` | `OnCompleteOperation` | `{cameraId, operation, result}` | Generic SDK operation completed (e.g. control codes, non-shutter commands) |
         *     | `error` | `OnError` | `{cameraId, code, message}` | SDK error occurred |
         *
         *     ## ⚠ The dual `connected` event quirk
         *
         *     Two distinct conditions emit an event named `connected`:
         *
         *     1. **Initial SSE handshake** — fired immediately when the EventSource
         *        connects, with payload `{"cameraId":"<subscription-id-or-*>"}`. Use
         *        it to confirm the SSE channel is open. NOT a camera state change.
         *     2. **Real camera connect** — fired when the SDK's `OnConnected`
         *        callback fires for a camera, with the full camera info payload.
         *
         *     Discriminate by payload shape if you need to distinguish them. The
         *     cleanest pattern is to ignore the first `connected` event after
         *     opening the EventSource and treat any subsequent `connected` as a
         *     real camera-state change.
         *
         *     ## Example
         *
         *     ```javascript
         *     const events = new EventSource("http://localhost:8080/api/events");
         *     events.addEventListener("downloadComplete", (e) => {
         *       console.log("File saved:", JSON.parse(e.data).savedPath);
         *     });
         *     events.addEventListener("propertyChanged", (e) => {
         *       const { codes } = JSON.parse(e.data);
         *       // Refresh affected properties via getProperty / getAllProperties
         *     });
         *     ```
         */
        get: operations["subscribeAllEvents"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/events": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Subscribe to events for a specific camera (SSE)
         * @description **SDK generation note:** marked `x-fern-ignore: true` — see
         *     `subscribeAllEvents` for rationale.
         *
         *     Same event catalog as `subscribeAllEvents`, filtered server-side to a
         *     single camera ID. Useful when a single page only cares about one
         *     camera and you want to avoid filtering on the client.
         *
         *     See `subscribeAllEvents` for the full event catalog, payload shapes,
         *     and the dual-`connected` quirk.
         */
        get: operations["subscribeCameraEvents"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/sd-card/slot/{slotNumber}/files": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List SD card files
         * @description List files on the SD card. Requires `remote-transfer` or `contents` mode.
         *     Returns 400 if called in `remote` mode.
         */
        get: operations["listSDCardFiles"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/sd-card/slot/{slotNumber}/files/{contentId}/{fileId}/download": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Download file from SD card
         * @description Initiates an async download of a specific file from the SD card to the host PC.
         *     Returns immediately; completion is signaled via the `downloadComplete` SSE event
         *     (remote/remote-transfer modes) or via filesystem polling (contents-transfer mode).
         */
        post: operations["downloadSDCardFile"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/sd-card/slot/{slotNumber}/files/{contentId}/{fileId}/thumbnail": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Download thumbnail (small JPEG preview)
         * @description Initiates an async download of the file's thumbnail (small JPEG preview).
         *     Used for fast file browser previews. Remote-transfer mode only.
         */
        post: operations["downloadSDCardThumbnail"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/sd-card/slot/{slotNumber}/files/{contentId}/{fileId}/screennail": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Download screennail (medium JPEG preview)
         * @description Initiates an async download of the file's screennail (medium JPEG preview,
         *     larger than thumbnail). Useful for ML evaluation and quality previews.
         *     Remote-transfer mode only.
         */
        post: operations["downloadSDCardScreennail"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/settings/save-info": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get save path configuration
         * @description Returns the current save directory, filename prefix, and start number for auto-transferred images.
         */
        get: operations["getSaveInfo"];
        /**
         * Set save path configuration
         * @description Configure the save directory, filename prefix, and start number.
         */
        put: operations["setSaveInfo"];
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/settings/download": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Save camera settings to PC
         * @description Download the camera's current settings to a file on the host PC.
         *
         *     The file is written to a `camera_settings/` directory relative to the
         *     **server's** working directory — not a caller-supplied path. There is no
         *     `save_path` for this endpoint (unlike the SD card endpoints); the optional
         *     `filename` only names the file. The response `data.path` reports where it
         *     landed. A typical file is `CUMSET.DAT`, around 180 KB.
         *
         *     Completion also emits a `downloadComplete` SSE event.
         *
         *     ## Requires the camera to allow it
         *
         *     The SDK only permits this when the camera reports
         *     `camera-setting-save-enable` as `1` and `camera-setting-save-read-state`
         *     as `0` (Idle). Read those via `getAllProperties` before calling — a `400`
         *     here usually means the camera is refusing, not that the request was
         *     malformed.
         *
         *     The camera will not enable it without recording media present. On bodies
         *     with two card slots this means **slot 1** specifically: an ILCE-7M4 with a
         *     card in slot 2 only reports `camera-setting-save-enable: 0` and rejects
         *     the call.
         */
        post: operations["downloadCameraSettings"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/settings/upload": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Load settings to camera
         * @description Upload a previously saved settings file back to the camera. `filename` is
         *     required and is resolved inside the server's `camera_settings/` directory —
         *     use `listCameraSettingsFiles` to see what is available.
         *
         *     **The camera reboots and drops the connection** once the settings are
         *     applied. Reconnect afterwards; the response carries this as
         *     `data.warning`. The result arrives as an SSE `settingsResult` event with
         *     `{"operation":"read","success":true}`.
         *
         *     ## Requires the camera to allow it
         *
         *     Gated the same way as `downloadCameraSettings`, but on
         *     `camera-setting-read-enable` being `1` (with
         *     `camera-setting-save-read-state` at `0`). Recording media must be present —
         *     **slot 1** on two-slot bodies. Read those properties via
         *     `getAllProperties` to tell a refusing camera from a bad request.
         */
        post: operations["uploadCameraSettings"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/settings/files": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List saved settings files
         * @description Lists camera settings files (`*.DAT`) previously saved to the host PC
         *     via `downloadCameraSettings`. This is a host-side file listing; the
         *     path currently includes `cameraId` for consistency with the rest of
         *     the API, but the handler does not validate that camera.
         */
        get: operations["listSettingsFiles"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/cameras/{cameraId}/lut/import": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Import a .cube LUT file to the camera
         * @description Initiates an async import of a `.cube` LUT file from the host filesystem
         *     into one of the camera's user LUT slots (1–16). CineEI / cinema bodies only.
         */
        post: operations["importLUT"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /** @description Camera discovered during enumeration */
        CameraInfo: {
            /** @example D10F60149B0C */
            id: string;
            /** @example ILCE-9M3 */
            model: string;
            /** @enum {string} */
            connectionType: "USB" | "Network";
            connected: boolean;
        };
        /** @description Camera info embedded in API responses */
        CameraSummary: {
            /** @example false */
            connected?: boolean;
            /** @example ILCE-7M4 */
            model?: string;
            /** @example D06CE05E3323 */
            id?: string;
        };
        /**
         * @description Generic envelope returned by most camera operations. The shape of
         *     `data` varies per endpoint — most operations return an empty or
         *     null `data`; operations that return structured data document a
         *     specific shape. SDK-generated types can treat `data` as an opaque
         *     map for generic endpoints.
         */
        CameraResponse: {
            success: boolean;
            message?: string;
            camera?: components["schemas"]["CameraSummary"];
            data?: {
                [key: string]: unknown;
            };
        };
        /**
         * @description Response envelope for `GET /api/cameras/{id}/connection`. When the
         *     camera is connected, `data.mode` reports the active SDK connection
         *     mode (`remote` / `remote-transfer` / `contents`) so clients don't
         *     have to remember what they passed at connect time. When the camera
         *     is not connected, `data` is omitted.
         */
        ConnectionStatusResponse: {
            success: boolean;
            message?: string;
            camera?: components["schemas"]["CameraSummary"];
            data?: {
                mode?: components["schemas"]["ConnectionMode"];
            };
        };
        CameraListResponse: {
            success: boolean;
            message?: string;
            cameras: components["schemas"]["CameraInfo"][];
        };
        GetPropertyResponse: {
            success: boolean;
            message?: string;
            camera?: components["schemas"]["CameraSummary"];
            data: components["schemas"]["PropertyData"];
        };
        GetAllPropertiesResponse: {
            success: boolean;
            message?: string;
            camera?: components["schemas"]["CameraSummary"];
            data: components["schemas"]["BulkPropertiesData"];
        };
        GetPriorityKeyResponse: {
            success: boolean;
            message?: string;
            camera?: components["schemas"]["CameraSummary"];
            data: components["schemas"]["PriorityKeyData"];
        };
        GetLiveViewStatusResponse: {
            success: boolean;
            message?: string;
            camera?: components["schemas"]["CameraSummary"];
            data: components["schemas"]["LiveViewStatus"];
        };
        GetOSDStatusResponse: {
            success: boolean;
            message?: string;
            camera?: components["schemas"]["CameraSummary"];
            data: components["schemas"]["OSDStatus"];
        };
        GetSaveInfoResponse: {
            success: boolean;
            message?: string;
            camera?: components["schemas"]["CameraSummary"];
            data?: components["schemas"]["SaveInfoData"];
        };
        ServerLogsResponse: {
            success: boolean;
            logs: components["schemas"]["LogEntry"][];
            /** @description Total buffered log count */
            total?: number;
        };
        ShutdownServerResponse: {
            success: boolean;
            message?: string;
        };
        SetPriorityKeyRequest: {
            setting: components["schemas"]["PriorityKeySetting"];
        };
        /**
         * @description Standard non-2xx response envelope. The API does not currently expose a
         *     stable machine-readable error code in the JSON body, so clients should
         *     branch on HTTP status first and then inspect `message` for the
         *     human-readable reason.
         * @example {
         *       "success": false,
         *       "message": "Camera not connected",
         *       "camera": {
         *         "connected": false,
         *         "model": "ILCE-7M4",
         *         "id": "D06CE05E3323"
         *       }
         *     }
         */
        ErrorResponse: {
            /**
             * @example false
             * @constant
             */
            success: false;
            /** @example Camera not connected */
            message: string;
            camera?: components["schemas"]["CameraSummary"];
        };
        /**
         * @description Single-property GET payload returned under `data` by
         *     `GET /api/cameras/{id}/properties/{name}`.
         */
        PropertyData: {
            /** @description REST property name (matches `PropertyName` enum) */
            property: string;
            /**
             * @description Hex-encoded current value (use this in SET requests)
             * @example 0x1007d
             */
            value: string;
            /**
             * @description Human-readable display value (also accepted by SET)
             * @example 1/125
             */
            formatted: string;
            /**
             * @description Whether this property is writable **in the camera's current state**.
             *     Depends on the priority key: with `camera-position` held most of the
             *     body reports `false`. Claim `pc-remote` before treating this as a
             *     statement about the model. See `getProperty` for the full caveat.
             */
            writable: boolean;
            /**
             * @description Values the camera accepts **right now**. Empty for properties with no
             *     enumerated values (ranges, freeform strings), and also when the
             *     priority key blocks writes. The set can shrink or grow as other
             *     properties change — `focus-area` loses its Tracking variants when
             *     `focus-mode` is not continuous — so re-read after related changes
             *     rather than caching.
             */
            available_values?: components["schemas"]["AvailableValue"][];
        };
        /**
         * @description Single entry inside the `available_values` array returned by single-GET
         *     property responses. Contains only the hex value (as a string) and the
         *     formatted label — no separate numeric value field. The bulk endpoint
         *     uses `BulkAvailableValue`, which is a different shape.
         */
        AvailableValue: {
            /**
             * @description Hex value to use in SET requests
             * @example 0x11f40
             */
            value: string;
            /**
             * @description Human-readable label
             * @example 1/8000
             */
            formatted: string;
        };
        /**
         * @description Per-property entry inside the `data.properties` map returned by
         *     `GET /api/cameras/{id}/properties/all`. Note that this schema is NOT
         *     the same as `PropertyData`:
         *
         *     - Field names are prefixed with `current_` (`current_value`,
         *       `current_hex_value`, `current_formatted`)
         *     - `writable` is a real JSON boolean, not a stringified one
         *     - Each entry in `available_values` includes BOTH a numeric `value`
         *       and a `hex_value` string
         *
         *     This is because the bulk handler serializes property data inline rather
         *     than going through the string-string `data` map.
         */
        BulkPropertyEntry: {
            /**
             * Format: double
             * @description Numeric SDK value. Typed as `number` (rather than `int64`) because
             *     some properties (e.g. `exposure-compensation`, `image-id-num`) emit
             *     UInt64-range values on the wire that don't fit in a signed 64-bit
             *     integer. Treat this field as opaque — use `current_hex_value` in
             *     SET requests.
             * @example 100
             */
            current_value: number;
            /** @example 0x1007d */
            current_hex_value: string;
            /** @example 1/125 */
            current_formatted: string;
            writable: boolean;
            available_values: components["schemas"]["BulkAvailableValue"][];
        };
        BulkAvailableValue: {
            /**
             * Format: double
             * @description Numeric SDK value. Typed as `number` (rather than `int64`) because
             *     some properties emit UInt64-range values on the wire that don't fit
             *     in a signed 64-bit integer. Treat this field as opaque — use
             *     `hex_value` in SET requests.
             * @example 32000
             */
            value: number;
            /** @example 0x11f40 */
            hex_value: string;
            /** @example 1/8000 */
            formatted: string;
        };
        /**
         * @description Wrapper object inside the bulk-GET `data` field. The
         *     `properties` member is a map of property name → entry.
         */
        BulkPropertiesData: {
            /** @description Map of property name (matches `PropertyName`) to its current value entry */
            properties: {
                [key: string]: components["schemas"]["BulkPropertyEntry"];
            };
            /** @description Total number of property entries the camera reported (BEFORE deduplication). */
            total_properties: number;
        };
        PriorityKeyData: {
            setting?: components["schemas"]["PriorityKeySetting"];
            rawValue?: string;
        };
        LiveViewStatus: {
            /** @description Whether live view is enabled on the camera body. */
            enabled?: boolean;
            /** @description Whether the server is currently capturing frames. */
            streaming?: boolean;
        };
        OSDStatus: {
            /** @description Whether the connected camera body supports OSD composite frames. */
            supported?: boolean;
            /** @description Whether OSD mode is currently enabled. */
            enabled?: boolean;
        };
        SaveInfoData: {
            path?: string;
            prefix?: string;
            /** @description Starting file number. */
            startNo?: number;
        };
        SDCardFile: {
            /** @description SDK content handle for use in download requests */
            content_id: number;
            /**
             * @description SDK file handle. Always `0` in contents-transfer mode (one file per content).
             *     Multi-file content (e.g. RAW+JPEG pair) returns separate entries with the
             *     same `content_id` and distinct `file_id` values.
             */
            file_id: number;
            /** @example DCIM/100MSDCF/DSC00001.ARW */
            file_path: string;
            /**
             * Format: int64
             * @description File size in bytes
             */
            file_size: number;
            /** @description Image width in pixels (contents-transfer mode only) */
            width?: number;
            /** @description Image height in pixels (contents-transfer mode only) */
            height?: number;
            creation_year?: number;
            creation_month?: number;
            creation_day?: number;
            creation_hour?: number;
            creation_minute?: number;
            creation_second?: number;
        };
        SDCardFileListResponse: {
            success: boolean;
            slot: number;
            file_count: number;
            files: components["schemas"]["SDCardFile"][];
        };
        /** @description Optional save path override. If omitted, uses the connection-time `SetSaveInfo` path. */
        SDCardDownloadRequest: {
            /**
             * @description Directory path on host PC to save the file
             * @example /Users/me/Pictures/captures
             */
            save_path?: string;
        };
        /**
         * @description Generic envelope for async operations that have been accepted but not yet completed.
         *     Watch for the corresponding SSE event (`downloadComplete`, `transferProgress`) to
         *     learn when the operation finishes.
         */
        AsyncOperationResponse: {
            success: boolean;
            message: string;
        };
        /** @description Response envelope for thumbnail/screennail downloads. */
        CompressedDownloadResponse: {
            success: boolean;
            message: string;
            /** @enum {string} */
            type?: "thumbnail" | "screennail";
        };
        ConnectionRequest: {
            mode?: components["schemas"]["ConnectionMode"];
            auth?: components["schemas"]["ConnectionAuth"];
            /**
             * @deprecated
             * @description Deprecated — use `auth.username`. Still honoured, but it cannot carry
             *     a fingerprint, so on its own it is not enough for a body with access
             *     authentication switched on. `auth` wins when both are present.
             */
            username?: string;
            /**
             * @deprecated
             * @description Deprecated — use `auth.password`.
             */
            password?: string;
            /**
             * @description Auto-reconnect on disconnection
             * @enum {string}
             */
            reconnecting?: "on" | "off";
        };
        /**
         * @description Credentials for a camera with **access authentication** switched on.
         *
         *     Omit this entirely for USB, and for networked bodies with access
         *     authentication off — that is the ordinary case and it connects
         *     unauthenticated exactly as before.
         *
         *     A body that requires authentication needs all three fields together.
         *     Verified on an ILCE-1M2 over Ethernet: the camera checks its own
         *     fingerprint first and the credentials second, so the two failures are
         *     reported separately.
         *
         *     Credentials are never echoed in a response and never written to the
         *     server log — only whether authentication was attempted.
         */
        ConnectionAuth: {
            /** @description Access-authentication user, as shown on the camera. */
            username?: string;
            /**
             * Format: password
             * @description Access-authentication password, as shown on the camera.
             */
            password?: string;
            /**
             * @description The camera's fingerprint. **Get this from `getFingerprint`, not by
             *     reading it off the camera screen** — the SDK's rendering is what the
             *     connect check compares against, and a transcribed value is easy to
             *     get subtly wrong. A single missing `=` of base64 padding is enough to
             *     fail with `0x8215`.
             */
            fingerprint?: string;
        };
        PropertySetRequest: {
            /**
             * @description Hex value from `available_values` (e.g. `"0x1007d"`), human-readable
             *     string (e.g. `"1/125"`, `"f/5.6"`, `"wide"`), or decimal string.
             */
            value: string;
        };
        ButtonRequest: {
            /**
             * @description Which button to press. Not every body accepts every name — a `400`
             *     response lists what the connected camera reports as operable.
             * @enum {string}
             */
            button: "up" | "down" | "left" | "right" | "enter" | "menu" | "multi-selector-up" | "multi-selector-down" | "multi-selector-left" | "multi-selector-right" | "multi-selector-enter" | "multi-selector-right-up" | "multi-selector-right-down" | "multi-selector-left-up" | "multi-selector-left-down" | "fn" | "playback" | "delete" | "mode" | "c1" | "c2" | "c3" | "c4" | "c5" | "c6" | "c7" | "movie" | "ael" | "af-on" | "home" | "clips" | "slot-select" | "display" | "cancel-back" | "thumbnail";
            /**
             * @description `down` to hold and `up` to release. Omit (or send `press`) for a full
             *     down-then-up. An `up` is always accepted so a held key can be
             *     released.
             * @enum {string}
             */
            action?: "press" | "down" | "up";
        };
        ShutterRequest: {
            /**
             * @description For continuous shooting: `down` to start, `up` to stop.
             *     Omit for single shot.
             * @enum {string}
             */
            action?: "down" | "up";
        };
        /**
         * @description Power zoom command. The server accepts two forms:
         *
         *     - **Direction + speed preset** (`ZoomDirectionalRequest`):
         *       `{ "direction": "in" | "out", "speed": "normal" | "fast" }`.
         *     - **Signed speed** (`ZoomSignedSpeedRequest`):
         *       `{ "speed": -10..+10 }`. Negative = zoom out, positive = zoom in,
         *       magnitude = speed. `0` stops the zoom.
         *
         *     The two forms are disjoint on the `speed` field's JSON type, so
         *     generated SDKs may either expose them as an untagged union or
         *     surface one method per form (see `x-fern-sdk-method-name` on the
         *     endpoint).
         */
        ZoomRequest: components["schemas"]["ZoomDirectionalRequest"] | components["schemas"]["ZoomSignedSpeedRequest"];
        /** @description Direction + named speed preset. */
        ZoomDirectionalRequest: {
            /** @enum {string} */
            direction: "in" | "out";
            /** @enum {string} */
            speed: "normal" | "fast";
        };
        /**
         * @description Signed speed in range `-10..+10`. Positive = zoom in, negative =
         *     zoom out, magnitude = speed. `0` stops an in-progress zoom.
         */
        ZoomSignedSpeedRequest: {
            speed: number;
        };
        FocusNearFarRequest: {
            /** @description Negative = near, positive = far. Magnitude controls speed. 0 is invalid. */
            step: number;
        };
        SaveInfoRequest: {
            /** @description Directory path on host PC */
            path?: string;
            /** @description Filename prefix */
            prefix?: string;
            /** @description Starting file number */
            startNo?: number;
        };
        /**
         * @description Supply either `x` and `y`, or a `normalized` object. If both are present,
         *     `normalized` wins.
         */
        AFAreaPositionRequest: {
            /** @description Frame centre X in the camera's AF coordinate space. */
            x?: number;
            /** @description Frame centre Y in the camera's AF coordinate space. */
            y?: number;
            /**
             * @description Frame centre as 0-1 fractions of the live view — use this for a tap or
             *     click position without converting to the SDK's coordinate space.
             */
            normalized?: {
                x?: number;
                y?: number;
            };
        };
        /** @description A single autofocus frame as the camera reports it. */
        AFFrame: {
            /**
             * @description Frame type, e.g. `contrast-flexible-main`, `contrast-flexible-assist`,
             *     `phase-detection-image-sensor`, `wide`, `zone`, `central-emphasis`,
             *     `contrast`, `frame-somewhere`, `unknown`.
             */
            type?: string;
            /**
             * @description `focused`, `not-focused`, `moving`, `selection`, `registration-af`,
             *     `island` or `unknown`. Drive the box colour from this.
             */
            state?: string;
            priority?: number;
            /** @description Frame centre as 0-1 fractions — use this to place an overlay. */
            normalized?: {
                x?: number;
                y?: number;
            };
            /**
             * @description The same centre in the coordinate space `setAFAreaPosition` accepts, so
             *     a value read here can be written straight back.
             */
            position?: {
                x?: number;
                y?: number;
            };
            /** @description Box dimensions, raw and as a fraction of the frame. */
            size?: {
                width?: number;
                height?: number;
                normalized_width?: number;
                normalized_height?: number;
            };
            /** @description The camera's own numerator/denominator pair, unconverted. */
            raw?: {
                xNumerator?: number;
                xDenominator?: number;
                yNumerator?: number;
                yDenominator?: number;
            };
        };
        AFAreaPositionResponse: {
            success?: boolean;
            message?: string;
            data?: {
                /** @description Whether the camera reported a focus frame at all. */
                available?: boolean;
                frame_count?: number;
                /**
                 * @description One entry per frame. Expanding and tracking areas report a main
                 *     box plus an assist box — render every entry.
                 */
                frames?: components["schemas"]["AFFrame"][];
            };
        };
        /**
         * @description Supply either `x` and `y`, or a `normalized` object. If both are present,
         *     `normalized` wins. Same coordinate space as `AFAreaPositionRequest`.
         */
        TouchRequest: {
            /** @description Touch point X in the camera's coordinate space. */
            x?: number;
            /** @description Touch point Y in the camera's coordinate space. */
            y?: number;
            /**
             * @description Touch point as 0-1 fractions of the live view — use this for a tap or
             *     click position without converting to the SDK's coordinate space.
             */
            normalized?: {
                x?: number;
                y?: number;
            };
        };
        /** @description A single tracking frame as the camera reports it. */
        TrackingFrame: {
            /**
             * @description `target-af` for the subject being tracked, `non-target-af` for a
             *     candidate the camera is showing but not acting on, or `unknown`.
             */
            type?: string;
            /**
             * @description `focused`, `not-focused`, `moving`, `selection`, `registration-af`,
             *     `island` or `unknown` — the same states the AF frame reports.
             */
            state?: string;
            priority?: number;
            /** @description Frame centre as 0-1 fractions — use this to place an overlay. */
            normalized?: {
                x?: number;
                y?: number;
            };
            /**
             * @description The same centre in the coordinate space `remoteTouch` accepts, so a
             *     tracked position can be fed straight back in.
             */
            position?: {
                x?: number;
                y?: number;
            };
            /** @description Box dimensions, raw and as a fraction of the frame. */
            size?: {
                width?: number;
                height?: number;
                normalized_width?: number;
                normalized_height?: number;
            };
            /** @description The camera's own numerator/denominator pair, unconverted. */
            raw?: {
                xNumerator?: number;
                xDenominator?: number;
                yNumerator?: number;
                yDenominator?: number;
            };
        };
        FingerprintResponse: {
            success?: boolean;
            message?: string;
            data?: {
                /**
                 * @description Whether this body uses access authentication. When `false`, no
                 *     fingerprint is returned and none is needed to connect.
                 */
                ssh_supported?: boolean;
                /**
                 * @description Pass verbatim as `auth.fingerprint`, including any trailing `=`.
                 *     Absent when `ssh_supported` is `false`.
                 */
                fingerprint?: string;
                /** @description Character count, so a caller can spot a truncated copy-paste. */
                length?: number;
            };
        };
        TrackingFrameResponse: {
            success?: boolean;
            message?: string;
            data?: {
                /**
                 * @description Whether the tracking property could be read. True with an empty
                 *     `frames` array means nothing is being tracked.
                 */
                available?: boolean;
                frame_count?: number;
                /** @description One entry per frame the camera reports. */
                frames?: components["schemas"]["TrackingFrame"][];
            };
        };
        SettingsFileRequest: {
            /** @description Settings filename (e.g. `CUMSET.DAT`) */
            filename?: string;
        };
        SettingsFileListResponse: {
            success: boolean;
            message?: string;
            files?: string[];
            count?: number;
        };
        LUTImportRequest: {
            /**
             * @description Absolute or working-directory-relative path to a `.cube` file on the host
             * @example /Users/me/luts/example.cube
             */
            filePath: string;
            /**
             * @description Camera user LUT slot to import into
             * @default 1
             */
            slot: number;
        };
        LUTImportResponse: {
            success: boolean;
            message: string;
            data?: {
                slot?: number;
                file?: string;
            };
        };
        ServerStatus: {
            success: boolean;
            server: {
                /**
                 * @description Server binary version (hardcoded in `handleApiServerStatus`).
                 *     Distinct from the OpenAPI spec version in `info.version`.
                 * @example 3.0.0
                 */
                version: string;
                /**
                 * @description Version of the Sony Camera Remote SDK this binary is linked
                 *     against, read at runtime via `GetSDKVersion()`. It reflects the
                 *     library actually loaded, so it tracks an SDK upgrade without a
                 *     code change.
                 * @example V2.02.00
                 */
                sdkVersion: string;
                /** @description Server uptime in seconds */
                uptime: number;
                /**
                 * @description Host OS platform identifier
                 * @example darwin-arm64
                 * @enum {string}
                 */
                platform: "darwin-arm64" | "darwin-x64" | "linux-arm64" | "linux-arm" | "linux-x64" | "win32-x64" | "unknown";
            };
            cameras: {
                /** @description Number of currently connected cameras */
                connected: number;
                /** @description Number of discovered (available) cameras */
                discovered: number;
            };
        };
        LogEntry: {
            /** Format: date-time */
            timestamp?: string;
            /** @enum {string} */
            level?: "debug" | "info" | "warn" | "error";
            message?: string;
            /** @description Camera ID if log is camera-specific */
            cameraId?: string;
        };
        /**
         * @description - `remote` — Full control + auto-transfer of captured images to host PC
         *     - `remote-transfer` — Full control + explicit SD card file access
         *     - `contents` — SD card file access only (no shooting or settings control)
         * @enum {string}
         */
        ConnectionMode: "remote" | "remote-transfer" | "contents";
        /**
         * @description Canonical REST property names. Mirrors the server's `PROPERTY_MAP`
         *     (`webapp/server/src/CameraWebController.cpp`). Use `GET /properties/all`
         *     to see the full set with current values, writability, and accepted ranges
         *     for the connected camera.
         *
         *     Some properties are read-only on every body (status / state fields).
         *     Others are read-only depending on shooting mode (e.g. `aperture` is
         *     read-only outside Manual / Aperture-priority modes). Always check the
         *     `writable` field on a single GET before attempting a SET.
         *
         *     ## Property tiers
         *
         *     **Core (R/W):** `iso`, `aperture`, `shutter-speed`, `drive-mode`,
         *     `white-balance`, `focus-mode`, `exposure-program-mode`,
         *     `still-image-store-destination`
         *
         *     **Image format:** `file-format`, `image-quality`, `raw-compression`
         *
         *     **Focus:** `focus-area`, `focus-distance` (read), `focus-position`,
         *     `focus-position-current` (read), `focus-driving-status` (read)
         *
         *     **Zoom:** `zoom-distance` (read), `zoom-setting`
         *
         *     **Battery / power:** `battery-remain` (read)
         *
         *     **Video state / format:** `recording-state` (read), `movie-file-format`,
         *     `movie-recording-setting`, `movie-recording-frame-rate`
         *
         *     **Thermal / errors:** `overheating-state` (read),
         *     `camera-error-caution-status` (read), `system-error-caution-status` (read),
         *     `camera-system-error-info` (read)
         *
         *     **Camera-settings file gates (read):** `camera-setting-save-enable`,
         *     `camera-setting-read-enable`, `camera-setting-save-read-state` — whether
         *     the camera will accept `downloadCameraSettings` / `uploadCameraSettings`.
         *     Both enable flags are `0` Disable / `1` Enable; the state is `0` Idle /
         *     `1` Reading. Check these when either endpoint returns 400.
         *
         *     **Media slots:** `media-slot1-status`, `media-slot1-remaining-photos`,
         *     `media-slot1-remaining-time`, `media-slot2-status`,
         *     `media-slot2-remaining-photos`, `media-slot2-remaining-time` (all read)
         *
         *     **Exposure:** `exposure-compensation`, `metering-mode`, `ae-lock`,
         *     `exposure-step`
         *
         *     **Flash:** `flash-mode`, `flash-compensation`, `wireless-flash`
         *
         *     **Shutter / silent:** `shutter-type`, `shutter-mode`, `silent-mode`,
         *     `shutter-angle`
         *
         *     **Image:** `image-size`, `aspect-ratio`, `color-space`, `dro`, `high-iso-nr`
         *
         *     **WB fine-tune:** `awb-lock`, `white-balance-color-temp`
         *
         *     **Creative / picture profile:** `creative-look`, `flicker-less-shooting`,
         *     `picture-profile`, `pp-lut-base-look`
         *
         *     **Audio:** `audio-recording`, `audio-input-master-level`
         *
         *     **Timecode:** `timecode-preset`, `timecode-format`, `timecode-run`,
         *     `timecode-make`
         *
         *     **Stabilization / NR:** `image-stabilization`, `movie-stabilization`,
         *     `long-exposure-nr`
         *
         *     **CineEI / LUT:** `movie-shooting-mode`, `movie-shooting-mode-color-gamut`,
         *     `base-look-import-enable`, `embed-lut-file`, `base-look-value`,
         *     `shooting-enable`, `exposure-index`, `base-iso`, `aps-c-s35`
         *
         *     **Image ID (MakerNote):** `image-id-string`, `image-id-string-max-length`,
         *     `image-id-num`, `image-id-num-setting`
         *
         *     **Priority:** `priority-key` (also exposed as `/priority-key` resource)
         * @enum {string}
         */
        PropertyName: "priority-key" | "iso" | "aperture" | "shutter-speed" | "drive-mode" | "white-balance" | "focus-mode" | "exposure-program-mode" | "still-image-store-destination" | "file-format" | "image-quality" | "raw-compression" | "focus-area" | "focus-distance" | "focus-position" | "focus-position-current" | "focus-driving-status" | "zoom-distance" | "zoom-setting" | "battery-remain" | "recording-state" | "movie-file-format" | "movie-recording-setting" | "movie-recording-frame-rate" | "overheating-state" | "camera-error-caution-status" | "system-error-caution-status" | "camera-system-error-info" | "camera-setting-save-enable" | "camera-setting-read-enable" | "camera-setting-save-read-state" | "media-slot1-status" | "media-slot1-remaining-photos" | "media-slot1-remaining-time" | "media-slot2-status" | "media-slot2-remaining-photos" | "media-slot2-remaining-time" | "exposure-compensation" | "metering-mode" | "ae-lock" | "exposure-step" | "flash-mode" | "flash-compensation" | "wireless-flash" | "shutter-type" | "shutter-mode" | "silent-mode" | "shutter-angle" | "image-size" | "aspect-ratio" | "color-space" | "dro" | "high-iso-nr" | "awb-lock" | "white-balance-color-temp" | "creative-look" | "flicker-less-shooting" | "picture-profile" | "pp-lut-base-look" | "audio-recording" | "audio-input-master-level" | "timecode-preset" | "timecode-format" | "timecode-run" | "timecode-make" | "image-stabilization" | "movie-stabilization" | "long-exposure-nr" | "movie-shooting-mode" | "movie-shooting-mode-color-gamut" | "base-look-import-enable" | "embed-lut-file" | "base-look-value" | "shooting-enable" | "exposure-index" | "base-iso" | "aps-c-s35" | "image-id-string" | "image-id-string-max-length" | "image-id-num" | "image-id-num-setting";
        /**
         * @description - `pc-remote` — Server has control (required for remote operation)
         *     - `camera-position` — Camera body has control
         * @enum {string}
         */
        PriorityKeySetting: "pc-remote" | "camera-position";
    };
    responses: {
        /**
         * @description Invalid request body or parameter shape. Use this when the JSON payload
         *     is malformed, a required field is missing, or a parameter cannot be
         *     parsed before any camera operation is attempted.
         */
        RequestValidationError: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /**
         * @description The request was syntactically valid, but the camera or server rejected
         *     it due to connection state, unsupported capability, property
         *     writability, current shooting mode, or another runtime precondition.
         */
        OperationRejected: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /**
         * @description The endpoint is valid, but not in the camera's current connection
         *     mode. Reconnect using the documented mode for this operation.
         */
        WrongConnectionMode: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /**
         * @description No frame is currently available. This usually means streaming has not
         *     been started yet, no frame has been captured yet, or OSD/composite
         *     prerequisites are not satisfied.
         */
        NoFrameAvailable: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
        /** @description Camera, file, or resource not found */
        NotFound: {
            headers: {
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["ErrorResponse"];
            };
        };
    };
    parameters: {
        /** @description Camera identifier (e.g. `D10F60149B0C`) */
        cameraId: string;
        /** @description Property name */
        propertyName: components["schemas"]["PropertyName"];
        /** @description SD card slot number (1 or 2) */
        slotNumber: 1 | 2;
        /** @description SDK content identifier from `listSDCardFiles` response */
        contentId: number;
        /** @description SDK file identifier from `listSDCardFiles` response (always `0` in contents-transfer mode) */
        fileId: number;
    };
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    getServerStatus: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Server is running */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ServerStatus"];
                };
            };
            /** @description Server error */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    getServerLogs: {
        parameters: {
            query?: {
                /** @description Number of recent log lines to return (default 100) */
                lines?: number;
                /** @description Minimum log level filter */
                level?: "debug" | "info" | "warn" | "error";
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Log entries */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ServerLogsResponse"];
                };
            };
        };
    };
    shutdownServer: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Shutdown initiated */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ShutdownServerResponse"];
                };
            };
            /** @description Shutdown failed */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    listCameras: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Camera list retrieved */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraListResponse"];
                };
            };
            /** @description SDK error during enumeration */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    getConnectionStatus: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Connection status */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ConnectionStatusResponse"];
                };
            };
        };
    };
    connectCamera: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["ConnectionRequest"];
            };
        };
        responses: {
            /** @description Camera connected */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            /**
             * @description Connection request rejected. This includes invalid `mode` values,
             *     no cameras discovered, target camera ID not present in the latest
             *     enumeration, all cameras already connected, or an SDK connect
             *     failure after a valid request.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    disconnectCamera: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Camera disconnected */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            400: components["responses"]["OperationRejected"];
        };
    };
    getProperty: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
                /** @description Property name */
                propertyName: components["parameters"]["propertyName"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Property retrieved */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GetPropertyResponse"];
                };
            };
            /**
             * @description Unknown property name, property getter not implemented, camera not
             *     connected, or property retrieval failed inside the SDK.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    setProperty: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
                /** @description Property name */
                propertyName: components["parameters"]["propertyName"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["PropertySetRequest"];
            };
        };
        responses: {
            /** @description Property set successfully */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            /**
             * @description Malformed request body, missing `value`, unknown property, invalid
             *     value format, property not writable, camera not connected, or the
             *     camera rejected the property set in its current state.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    getAllProperties: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description All properties retrieved */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GetAllPropertiesResponse"];
                };
            };
            /** @description Camera not connected or bulk property retrieval failed in the SDK */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    getAFAreaPosition: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description AF frame(s) retrieved */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AFAreaPositionResponse"];
                };
            };
            /**
             * @description No AF frame available — usually a focus area with no movable box, or
             *     live view not running.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Camera not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    setAFAreaPosition: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AFAreaPositionRequest"];
            };
        };
        responses: {
            /**
             * @description Accepted by the camera. The frame may have been clamped — confirm with
             *     `getAFAreaPosition`.
             */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            /**
             * @description Missing or malformed body, coordinates out of range, or the camera
             *     rejected the write.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Camera not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    getFingerprint: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /**
             * @description Fingerprint retrieved, or the camera does not use access
             *     authentication — check `ssh_supported`.
             */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["FingerprintResponse"];
                };
            };
            /**
             * @description The camera reports access authentication but the SDK could not read
             *     its fingerprint.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Camera not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    getTrackingFrame: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Tracking frame read. `frames` is empty when nothing is being tracked. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["TrackingFrameResponse"];
                };
            };
            /**
             * @description The tracking frame could not be read — usually live view is not
             *     running.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Camera not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    getPriorityKey: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Priority key retrieved */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GetPriorityKeyResponse"];
                };
            };
            400: components["responses"]["OperationRejected"];
        };
    };
    setPriorityKey: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SetPriorityKeyRequest"];
            };
        };
        responses: {
            /** @description Priority key set */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            /**
             * @description Invalid request body or the camera rejected the priority-key change.
             *     FX3 / cinema bodies may return SDK error `0x8402` because this
             *     property is not implemented there.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    triggerShutter: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["ShutterRequest"];
            };
        };
        responses: {
            /** @description Shutter action executed */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            400: components["responses"]["OperationRejected"];
        };
    };
    pressCameraButton: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ButtonRequest"];
            };
        };
        responses: {
            /** @description Button press sent */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            /**
             * @description Missing or unknown `button`, invalid `action`, or the camera refused
             *     the press because a key is already held. When the camera reported its
             *     operable keys they are listed in `data.supported_buttons`.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    remoteTouch: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["TouchRequest"];
            };
        };
        responses: {
            /**
             * @description Touch sent. Confirm what the camera locked onto with
             *     `getTrackingFrame`.
             */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            /**
             * @description Missing or malformed coordinates, coordinates out of range, or the
             *     camera reports remote touch as unavailable right now.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    cancelRemoteTouch: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Tracking cancelled */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            /** @description Nothing to cancel — the camera is not tracking a touched subject. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    halfPress: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Half-press executed */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            400: components["responses"]["OperationRejected"];
        };
    };
    afShutter: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description AF + shutter executed */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            /**
             * @description AF + shutter failed. Most common cause: camera is in Manual Focus
             *     mode. Response `data.error` is `"focus_mode_not_af"`.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    controlZoom: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ZoomRequest"];
            };
        };
        responses: {
            /** @description Zoom command executed */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            400: components["responses"]["OperationRejected"];
        };
    };
    toggleMovieRecording: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Movie recording toggled */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            /** @description Toggle failed (camera not in movie mode, not connected, etc.) */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    focusNearFar: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["FocusNearFarRequest"];
            };
        };
        responses: {
            /** @description Focus command executed */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            /**
             * @description Invalid step value (must be -7..-1 or +1..+7), focus near/far disabled
             *     on the lens, or the lens MF switch is in the wrong position.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    enableLiveView: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Live view enabled */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            400: components["responses"]["OperationRejected"];
        };
    };
    disableLiveView: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Live view disabled */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            400: components["responses"]["OperationRejected"];
        };
    };
    getLiveViewStatus: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Live view status */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GetLiveViewStatusResponse"];
                };
            };
            400: components["responses"]["OperationRejected"];
        };
    };
    startLiveViewStream: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Streaming started */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            400: components["responses"]["OperationRejected"];
        };
    };
    stopLiveViewStream: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Streaming stopped */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            400: components["responses"]["OperationRejected"];
        };
    };
    getLiveViewFrame: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description JPEG frame */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "image/jpeg": string;
                };
            };
            404: components["responses"]["NoFrameAvailable"];
        };
    };
    enableOSD: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OSD enabled */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            400: components["responses"]["OperationRejected"];
        };
    };
    disableOSD: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OSD disabled */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            400: components["responses"]["OperationRejected"];
        };
    };
    getOSDStatus: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OSD status */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GetOSDStatusResponse"];
                };
            };
            400: components["responses"]["OperationRejected"];
        };
    };
    getOSDFrame: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Composited JPEG frame */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "image/jpeg": string;
                };
            };
            /**
             * @description No OSD composite frame available. Ensure live view is streaming and
             *     OSD is enabled. OSD is not supported on every body.
             */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    subscribeAllEvents: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description SSE event stream */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "text/event-stream": string;
                };
            };
        };
    };
    subscribeCameraEvents: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description SSE event stream filtered to this camera */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "text/event-stream": string;
                };
            };
        };
    };
    listSDCardFiles: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
                /** @description SD card slot number (1 or 2) */
                slotNumber: components["parameters"]["slotNumber"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description File list retrieved */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SDCardFileListResponse"];
                };
            };
            400: components["responses"]["WrongConnectionMode"];
            /** @description Camera not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    downloadSDCardFile: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
                /** @description SD card slot number (1 or 2) */
                slotNumber: components["parameters"]["slotNumber"];
                /** @description SDK content identifier from `listSDCardFiles` response */
                contentId: components["parameters"]["contentId"];
                /** @description SDK file identifier from `listSDCardFiles` response (always `0` in contents-transfer mode) */
                fileId: components["parameters"]["fileId"];
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["SDCardDownloadRequest"];
            };
        };
        responses: {
            /** @description Download accepted (async) */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AsyncOperationResponse"];
                };
            };
            /** @description Download could not be started for the requested file */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Camera not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    downloadSDCardThumbnail: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
                /** @description SD card slot number (1 or 2) */
                slotNumber: components["parameters"]["slotNumber"];
                /** @description SDK content identifier from `listSDCardFiles` response */
                contentId: components["parameters"]["contentId"];
                /** @description SDK file identifier from `listSDCardFiles` response (always `0` in contents-transfer mode) */
                fileId: components["parameters"]["fileId"];
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["SDCardDownloadRequest"];
            };
        };
        responses: {
            /** @description Thumbnail download accepted (async) */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CompressedDownloadResponse"];
                };
            };
            /** @description Thumbnail download could not be started */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Camera not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    downloadSDCardScreennail: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
                /** @description SD card slot number (1 or 2) */
                slotNumber: components["parameters"]["slotNumber"];
                /** @description SDK content identifier from `listSDCardFiles` response */
                contentId: components["parameters"]["contentId"];
                /** @description SDK file identifier from `listSDCardFiles` response (always `0` in contents-transfer mode) */
                fileId: components["parameters"]["fileId"];
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["SDCardDownloadRequest"];
            };
        };
        responses: {
            /** @description Screennail download accepted (async) */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CompressedDownloadResponse"];
                };
            };
            /** @description Screennail download could not be started */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
            /** @description Camera not found */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    getSaveInfo: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Save info retrieved */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GetSaveInfoResponse"];
                };
            };
            400: components["responses"]["OperationRejected"];
        };
    };
    setSaveInfo: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SaveInfoRequest"];
            };
        };
        responses: {
            /** @description Save info set */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            /**
             * @description Malformed JSON body, missing required `path`, or the camera/server
             *     rejected the save-info update.
             */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    downloadCameraSettings: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["SettingsFileRequest"];
            };
        };
        responses: {
            /** @description Settings file saved */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            /** @description Camera settings download failed */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    uploadCameraSettings: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["SettingsFileRequest"];
            };
        };
        responses: {
            /** @description Settings uploaded to camera */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CameraResponse"];
                };
            };
            /** @description Missing filename in the request body or the settings upload failed. */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    listSettingsFiles: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Settings file list */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SettingsFileListResponse"];
                };
            };
            /** @description Host-side settings file listing failed */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
    importLUT: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description Camera identifier (e.g. `D10F60149B0C`) */
                cameraId: components["parameters"]["cameraId"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["LUTImportRequest"];
            };
        };
        responses: {
            /** @description LUT import accepted (async) */
            202: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["LUTImportResponse"];
                };
            };
            /** @description Invalid request (missing filePath, slot out of range, etc.) */
            400: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ErrorResponse"];
                };
            };
        };
    };
}
